# third-party sample of a deployed Reactor experience — reference only, not ours to publish
austerlitz/

# secrets
.env

node_modules/
dist/

# local voice-audition scratch: comparison pages and sample takes used to cast
# a voice. Disposable and media-heavy — regenerate rather than commit.
auditions*/
vo-preview.html
